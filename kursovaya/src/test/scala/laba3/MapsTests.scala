package laba3

import org.scalatest.FunSuite

class MapsTests extends FunSuite{
/*    test("GroupUsers") {
        val string = "aaa"
        val res = "AAA"
        val uppercase = Maps.testGroupUsers(string)
        assert(uppercase == res)
    }
    test("NumberFrodos") {
        val string = "aaa"
        val res = "AAA"
        val uppercase = Strings.testUppercase(string)
        assert(uppercase == res)
    }
    test("Underaged") {
        val string = "aaa"
        val res = "AAA"
        val uppercase = Strings.testUppercase(string)
        assert(uppercase == res)
    }*/
}